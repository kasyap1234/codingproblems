#given an array describe the frequencies of each element in the array ;

def highest_frequency(mylist):
    count={}
    for num in mylist:
        if num in count:
            count[num]+=1
        else:
            count[num]=1


print(highest_frequency([1,2,2,2,2,3,3,4,5,6]))
