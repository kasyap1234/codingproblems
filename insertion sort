def insertion_sort(thelist):

    for i in range(1,len(thelist)):
        value=list[i]
        j=i
        while j >0 and list[j-1]>value:
            list[j-1]=thelist[j]
            j=j-1
        value=list[j]
      return thelist
  
